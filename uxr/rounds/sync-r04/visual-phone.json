[
  {
    "scene": "sync-merged",
    "viewport": "phone",
    "severity": "minor",
    "where": "the '≠ Merged upstream · Tue' chip beside the Allergies badge at roughly 182–200 CSS px, and the Profile card's provenance line about 1,270 CSS px down the record",
    "finding": "The round-3 major is closed on the glass. Marcus arriving on Sunday gets the merge said four times before he can act on anything: the chip in the identity block, the heading 'This record was merged into another person', a paragraph that names the surviving person and states 'This page now reads from her', and a two-up that carries the two Planning Center ids rather than two values — which is the part that makes the silent case surface at all, since the surname is identical in both cells and only #AC377 against #AC101 says a person moved. What is left is what happens after he presses 'Got it'. The strip goes, and the only thing left in the top of the record is the chip — five words at 12px — whose full sentence lives in a `title` attribute that a thumb cannot open. The fact is still recoverable in real text ('Read from Ava Chen-Ito in Planning Center, since Tuesday's merge'), but it is in the Profile card about one and a half screens down, so the phone reader who dismissed the strip and then wonders why the allergy note reads differently pays a scroll that the pointer reader pays a hover for. Minor because the fact is on the page in text and because the chip is permanent, which is the protection that mattered.",
    "evidence": "sync-merged--phone.html: the chip is `<span class=\"uxr-job uxr-job-bad\" title=\"Planning Center merged this person into Ava Chen-Ito on Tuesday. Everything above is read from the survivor.\">` — the only carrier of the explanation once `state='merged'` stops rendering. `.uxr-job` is 12px below lg. Measured on sync-merged--phone-fold (780×1708 device, 2×): chip device x314–690, y364–400 = 157–345, 182–200 CSS; strip device y432–1280 = 216–640 CSS, 424 CSS px of the 854px fold. In sync-merged--phone-full the provenance line sits at roughly device y2540 ≈ 1270 CSS px, past one screen of scroll.",
    "direction": "The chip's sentence should not be pointer-only. Either the chip is the thing that scrolls to the provenance line when tapped, or 'Got it' collapses the strip to one line rather than to nothing. Do not solve it by making the chip bigger or by refusing to let it be dismissed — a state that cannot be acknowledged will be ignored instead."
  },
  {
    "scene": "sync-students",
    "viewport": "phone",
    "severity": "minor",
    "where": "the row chip column of the roster list — Petra Raman's '≠ Changed' at roughly 291 CSS px down sync-students--phone-fold, and the word a merged row would inherit",
    "finding": "The new state is drawn everywhere it has a strip and nowhere it has a chip. `merged` reuses `differs`' mark, which is right — but on the list a mark is never read alone, it is read as '≠ Changed', and that is the one place the merge and the value conflict are indistinguishable. Dana running J2 down forty-five rows sorts her list into three piles by the chip word: done, in flight, needs me. A row that says 'Changed' tells her somebody edited a field upstream and she has a two-value choice to make; a row that says the same thing but means the child now resolves to a different Planning Center person is a different errand and a heavier one. It is minor rather than major only because no merged row is rendered this round — the risk is carried entirely by the ideation note that 'merged maps to differs' entry' in the glyph map, which is true of the glyph and must not become true of the word.",
    "evidence": "sync-students--phone-fold and -full: the chip words in the list are the whole content a phone row gets — 'Queued', 'Waiting', 'Failed', 'Sending', 'Creating', 'Saved', 'Changed' — the desktop caption ('New record · Marcus Webb') is `hidden lg:` and never appears at 390px. On sync-outage--phone the same rows collapse to '! Held', which is correct there because ten rows share one cause. No merged row exists in either list this round.",
    "direction": "Share the mark, not the word: '≠ Merged' on the row, '≠ Changed' for the upstream value conflict. Nothing else about the chip changes and the glyph family stays closed, per the round-3 decline."
  }
]
