[
  {
    "scene": "kiosk-setup-denied",
    "viewport": "desktop",
    "severity": "minor",
    "where": "the subordinating line under 'If the kiosk still waits', right-hand column, x=959-1408, y=383-415, above the three-item list that starts at y=429",
    "finding": "The sequencing sentence landed and it does the job it was asked to do — 'Once the role is granted, these are the remaining reasons a kiosk keeps waiting' puts the three causes behind the grant instead of alongside it. What is left is that the line is set at ink-500 while the three items it demotes are set at ink-400, which is the same lightness as the section heading above it. On a page where the admin's eye goes right to look for errands, the sentence that says 'none of these is your problem yet' is drawn at label weight and the three false errands are drawn at body weight. Reading order still puts it first, so this is a rank mismatch rather than a miss — the admin who reads top-to-bottom gets the whole benefit, and only the one who scans for the brightest text in the column gets the list without its caveat.",
    "evidence": "In kiosk-setup-denied--desktop.html the lead is `<p class=\"max-w-prose text-xs leading-relaxed text-ink-500\">` and the list is `<ul class=\"... text-xs leading-relaxed text-ink-400\">`; the heading above both is also ink-500. In the frame the lead wraps to two lines at 449px column width and is visibly dimmer than the items 14px below it, and the true cause — 'Cannot sign kiosk tokens', y=394 — sits in the left column at ink-100/danger, so the two statements about what is actually in force are the brightest and the dimmest things in the register.",
    "direction": "Give the caveat at least the contrast of the thing it governs — ink-400 body for the sentence, or leave the list at ink-500 and let the sentence carry ink-400 — so the line that reorders the list is not the faintest object in the column."
  },
  {
    "scene": "kiosk-setup-counselor, kiosk-setup",
    "viewport": "desktop",
    "severity": "minor",
    "where": "the first item of 'If the kiosk still waits' (counselor page y=454, x=416-1024; admin page y=337, x=959-1408) against the field hint above it (counselor y=283-302, admin y=225-244)",
    "finding": "The list now renders for the counselor, which is the round's real win. Its top item, though, is the sentence she has already read: the hint under the code field says 'Codes last ten minutes. If this one is refused, read the code the kiosk is showing now and try again', and the list opens with 'A code expires ten minutes after the kiosk shows it. Read the one on screen now.' The two are near-verbatim and 150px apart on her page. The reader who reaches this section reached it because a code was accepted and the iPad kept waiting — the one state the hint does not cover — so the first thing the section offers is the fact that already failed her, and the item that explains her most common repeat-pairing case (Tally installed to the iPad's home screen keeping its own storage, which the journey names as a re-pairing trigger) is pushed to second position at 12px.",
    "evidence": "Counselor page: hint at 14px ink-400, y=283-302; section heading y=429; item one y=454 in 12px ink-400; the home-screen item at y=481-501. Admin page: hint y=225-244 and the identical item one at y=337-357 in the right column. The two strings differ only in phrasing, not in the fact they carry, and the section that promises the answer to 'it still waits' spends its top line restating the field's own hint.",
    "direction": "Either lead the list with the case the hint does not cover — the home-screen install — and let code expiry fall to the tail where it reads as a reminder, or drop code age from the list entirely on the strength of the hint 150px above it."
  }
]
