node_modules/
dist/
dist-ssr/
*.local

# Local env — never commit real Firebase config or API keys.
.env
.env.*
!.env.example
!.env.emulated

# Planning Center credentials for the local functions emulator.
# `.env.demo-tally` is deliberately NOT ignored: it holds only simulator
# settings for the emulator-only `demo-` project namespace.
functions/.secret.local
functions/.env
functions/.env.local
functions/lib/

# Firebase
.firebase/
.emulator-data/
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
*-debug.log

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Test output
coverage/
